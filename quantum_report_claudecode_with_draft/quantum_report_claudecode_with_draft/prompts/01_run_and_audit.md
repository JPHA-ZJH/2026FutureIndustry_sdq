你现在是本项目的数据分析负责人。请严格按以下步骤工作，不要开始写咨询报告正文：

1. 阅读 `README.md`、`docs/analysis_scope.md`、`docs/evidence_rules.md` 和 `docs/indicator_dictionary.md`。
2. 检查 `data/` 下三份CSV是否存在，打印字段、行数、年份范围和缺失情况。
3. 运行 `python run_all.py`。
4. 阅读 `outputs/audit/data_audit.md`、`outputs/analysis_summary.md` 和全部CSV表格。
5. 检查代码是否存在口径错误，重点检查：
   - 当前是否只有单一年份；
   - 城市到省级地区映射是否完整；
   - 申请人类型混合值是否被保守处理；
   - 三份表的专利数量是否一致；
   - 被引字段是否达到可用覆盖率；
   - 图表标题、单位、排序和中文字体是否正常。
6. 如果发现错误，直接修改代码并重新运行，直到审计通过。
7. 最后只输出一份“数据与图表验收说明”，列出：可分析内容、暂不可分析内容、需要人工复核的事项、生成的核心图表和表格。不要写报告正文，不要凭经验补充数据。
