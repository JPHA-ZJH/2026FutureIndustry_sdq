请先调试并运行本项目，暂时不要撰写咨询报告。

具体要求：

1. 阅读 `CLAUDE.md`、`README.md`、`config.py`、`scripts/prepare_data.py`、`scripts/analyze_report.py`。
2. 检查 `data/raw` 中四个CSV文件是否存在，读取字段、行数和年份范围。
3. 使用当前项目Python环境运行：

```bash
python run_all.py
```

4. 如报错，只做最小幅度修改，允许修复路径、字段、数据类型、空数据、中文字体和版本兼容问题；不得擅自修改技术路线映射、企业识别、地区范围和指标口径。
5. 运行成功后重点检查：
   - “上海市/上海”等城市是否统一；
   - 中国城市是否成功映射到省级地区，未映射值是否进入清单；
   - 第一申请人是否只有一个主体类型；
   - 合作专利的混合类型是否已根据第一申请人名称重新判断；
   - 医院是否单列为医疗机构；
   - 原始13类技术方向是否完整映射到7条报告技术路线；
   - 清洗后专利数、主体—年份表专利合计、主体汇总表专利合计是否一致；
   - 图14是否正确展示不同地区企业、高校、科研院所专利占比。
6. 检查全部图形可打开、中文无乱码、标题和图例不遮挡。
7. 最后汇报：代码是否成功、修改内容、审计结果、生成的表图以及仍需人工复核的问题。
8. 暂时不要写报告正文。
